const {test}=require('node:test'),assert=require('node:assert/strict');const {responseHeaders}=require('./response-headers.cjs');
test('decoded cloud payload is not labelled as compressed or given upstream length',()=>{const headers=responseHeaders({'content-encoding':'gzip','content-length':'42','transfer-encoding':'chunked','content-type':'application/json','cache-control':'no-store'});assert.equal(headers['content-encoding'],undefined);assert.equal(headers['content-length'],undefined);assert.equal(headers['transfer-encoding'],undefined);assert.equal(headers['content-type'],'application/json');assert.equal(headers['cache-control'],'no-store');});
