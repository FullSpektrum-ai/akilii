# ollama

Private Railway service. Do not assign a public domain.

The alpha.9 qualification image bakes in `qwen3:0.6b` so hosted runtime validation does not depend on a cloud provider API key or a first-request download.

Listens on port `11434` and is reached by FlowState at `http://ollama.railway.internal:11434`.
